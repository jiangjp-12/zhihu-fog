# 热帖迷雾：评论区伪人局 —— 产品说明计划书

> 知乎黑客松 2026 · 校园新锐季
> 线上 Demo：部署后填入 `https://zhihu-fog.pages.dev`
> 代码仓库：选填

---

## 一、创作思路

### 从一个真实的社区体验出发

在知乎刷评论区时，越来越常出现一种停顿：**这条评论，是人写的吗？**

它往下分成几种感觉——话说得太齐整、观点太四平八稳、每段都以"首先"开头、看着有理却没有任何个人经历。
这种判断发生在毫秒之间，靠的不是逻辑推理，而是**语感**：真人说话有毛刺、有情绪、会跑题、会突然自嘲。

这个判断能力很有意思：它是社区用户在长期阅读中自然长出来的，但从没有人**主动练过**，也没有地方能验证自己判断得准不准。

于是有了这个项目：**把"辨认伪人"这件已经在发生的事，做成一局有胜负的游戏。**

### 为什么是狼人杀结构

辨认 AI 天然是一个"不完全信息 + 社会推理"问题，和狼人杀同构：

- 有隐藏身份（谁是伪人）
- 有公开发言（评论就是唯一线索）
- 有集体决策（投票折叠）
- 有信息不对称（AI 之间也互不知情）

但它比狼人杀多了一层：**你要辨认的不是"谁在说谎"，而是"谁不是人"。**
狼人杀考察的是逻辑一致性，这个游戏考察的是语感。这是一个知乎用户特别擅长、且从未被游戏化的能力。

### 一个刻意的设计：AI 之间互不知情

规则里"两个伪人在黑夜独立选择、互相看不到"，是整个设计里最关键的一条。

它带来的结果是：**伪人的最优策略不是配合，而是各自像人。**
两个 AI 想折叠同一个人才能成功，但它们没有沟通渠道，只能各自去猜"最可能被同伴也盯上的人"——
也就是各自去猜"公共视野里最可疑的那个"。这让 AI 的行为被迫收敛到"读懂场上共识"，
而不是"和同伴打配合"。副产物是：黑夜经常平安无事，人类因此获得喘息，局势不会一边倒。

### 目标用户

| 人群 | 他们为什么会玩 |
|---|---|
| 知乎重度评论区用户 | 已经在下意识做这个判断，想验证自己准不准 |
| 对 AI 生成内容敏感的创作者 | 想知道 AI 现在能模仿到什么程度 |
| 想调 prompt 的开发者 / 学生 | 调试台是一个开箱可用的"拟人化 prompt 实验台" |
| 校园用户（黑客松定位） | 5 分钟一局，短、社交、有话题度 |

---

## 二、用户如何完成核心体验

一局约 7 分半，10 个阶段：

```
入场 15s      选圈子（科技/情感/职场/校园/法律），系统分配匿名昵称
帖子加载 20s  展示一条热点帖：标题 + 正文摘要 + 3 条高赞评论
讨论 3min     2 轮 × 90s。只聊热点，不聊 AI。楼中楼，可回复/赞/反对
识别 60s      回看全部发言（按人分组），私密标记"可信/可疑"——标记不影响投票
投票 30s      各 1 票，不能投自己，最高票出局，身份揭晓
辩解 30s      出局者发表最后辩解（辩解不保证真实）
黑夜 20s      存活伪人各自独立选择折叠对象，互不知情
天亮          同选中同一人 → 出局；选择不同 → 平安无事
终轮讨论 45s  存活者围绕"谁最可疑"简短交锋
终投 30s      最后一票，身份揭晓
结算 20s      揭晓全部身份 + 每人发言记录 + 被标记次数 + 投票命中率
复盘模式      房间不关闭，可回看全程、继续聊、再来一局
```

### 三个体验上的关键决定

**1. 讨论阶段禁止聊 AI。**
如果一开局就互相盘问"你是不是 AI"，游戏会立刻退化成审讯，且所有人都会开始表演。
强制前 3 分钟只聊热点，线索才是**自然产生**的——这也是真实评论区的状态。
识别阶段回看发言时，你看的是"他聊手机重量时怎么说话"，而不是"他被质问时怎么辩解"。

**2. 标记是私密的，且不影响投票。**
标记的作用是让你把判断**外化下来**，结算时再对照真实身份，看自己准不准。
它不进入任何计票逻辑，因此不会变成"公开站队"的工具。

**3. 出局不等于退出。**
出局者进入观战：能看存活者的发言与投票，有独立聊天频道，知道所有人身份，但不能发言/投票/标记。
这解决了社交游戏最大的体验断点——**死了就没事干了**。

---

## 三、技术方案

### 架构

```
浏览器（单文件 HTML）
  游戏状态机 / 渲染 / 刘看山过渡动画 / 调试台
  Tailwind CDN + 原生 JS，零构建步骤
        │
        ├──► LLM（OpenAI 兼容，用户自带 key，浏览器直连）
        │      未配置或调用失败 → 自动降级本地拟人模板
        │
        └──► Cloudflare Pages Functions（最小后端，仅为 OAuth 存在）
               /api/oauth/start     生成 state，跳转授权
               /api/oauth/callback  校验 state → 换 token → 建会话
               /api/me              返回登录用户（不含 token）
               /api/logout          清理会话
                     │
                     └──► openapi.zhihu.com（app_key 只在这一层出现）
```

**为什么必须有后端。** 单文件静态页和安全的 OAuth 不可兼得：`app_key` 一旦进入浏览器就等于公开。
所以游戏本体保持单文件（可离线双击运行），只把 token 交换和会话管理放进 Pages Functions。
Function 总代码约 200 行，不引入任何框架。

### OAuth 安全实现

- `app_id` / `app_key` / `redirect_uri` 全部通过 `wrangler pages secret` 注入，**不进源码、不进仓库**。
- `app_key` 与 OAuth `access_token` 只存在于服务端；浏览器只持有应用自己的随机会话 ID，
  走 `HttpOnly` + `Secure` + `SameSite=Lax`（HTTPS 下加 `__Host-` 前缀）。
- `state` 用 `crypto.getRandomValues` 生成，服务端保存并绑定当前浏览器（一次性 nonce cookie），
  10 分钟有效；回调时校验一致后**原子消费**。缺失 / 不匹配 / 过期 / 重复使用一律拒绝登录。
- Token 过期即停止读取，不静默切换到其他身份。

### 两处踩过的协议坑

开发中按官方 skill 文档的实测记录处理了两个与常规 OAuth 不同的地方：

1. **回调参数是 `authorization_code`，不是 `code`**，但换 token 时表单字段仍叫 `code`。
   接收端同时兼容两者，以 `authorization_code` 为主路径。
2. **`/user` 的 `uid` 是 int64，超出 JS 安全整数范围。** 直接 `JSON.parse` 会静默丢精度
   （`969570047710216200` → `...192`），且不报错。因此在解析**前**把长数字字面量改写为字符串，
   全程以字符串传递。这个坑不测不会发现，一旦踩中就是用户身份错乱。

另外 `/access_token` 与 `/user` 的业务字段 `code: 20000` 表示成功，
所以成功判定看的是"有没有拿到 token / 用户对象"，而不是 `code` 是否为零。

### AI 伪人策略

系统提示词的核心：

> 你是一个在知乎评论区混了五年的老用户，说话干脆，偶尔带点调侃。
> 短句优先，每句不超过 25 字。用反问代替断言。绝不承认自己是 AI。

配套约束：

- **温度按阶段动态调整**：讨论 0.88 / 终轮 0.8 / 辩解 0.7 / 识别 0.6 / 投票 0.55。
  讨论期要发散，投票期要稳。
- **套话黑名单**：首先、其次、最后、综上所述、值得注意的是、不难发现、由此可见——
  在输出后处理阶段正则剔除，因为这类词是 AI 最强的暴露特征。
- **允许带态度**，不必假装中立，可以抬杠、可以自嘲。中立恰恰是不像人的。
- **4 种人格**（科技圈 / 情感圈 / 职场圈 / 学生党）随圈子自动切换，各有语气特征词。
- **输出后处理**：去引号、去昵称前缀、去换行、超 60 字按句截断。

**降级设计**：未配置 API key 时走本地拟人模板（按阶段 + 人格取句，第二轮起掺入试探句），
游戏流程完全不受影响；配置后调用失败也自动降级并在日志中标注。
这保证评委打开就能玩完整一局，不依赖任何外部服务可用性。

### 调试台：本项目的第二个产出

游戏之外，这套东西本身是一个**评论区拟人化 prompt 实验台**：

- API 配置（base_url / key / model / temperature / max_tokens），预设 GroqCloud、Gemini、OpenRouter
- 系统提示词直接在界面编辑，**实时生效**，可一键恢复默认
- 每次调用记录 prompt 摘要、回复、耗时、温度、模式（API / 降级 / 本地模板）
- 玩家可对 AI 发言打「像人 / 不像人」，评分写入 logs
- 配置与日志均可导出 / 导入 JSON

也就是说：**每局游戏都是一次带人工标注的拟人化效果评测**。
`api_key` 只存本机 localStorage，导出时自动清空。

### 已做的假设与取舍

| 项 | 处理 |
|---|---|
| 刘看山素材 | 已接入官方刘看山 3D 动图（6 个 320×320 透明 GIF：待机/打招呼/晃悠/电脑/瞌睡/运球），按阶段映射。素材缺失时自动回落到手绘 SVG，保证离线可运行 |
| 托管域名 | 文档同样未提供，知乎只要求交可访问的 Demo 地址，因此自建 Cloudflare Pages |
| 多人实时 | 当前为单机原型（仅你是真人，其余 4 席由 LLM/模板驱动）。真多人需 WebSocket + 房间服务，超出单页范围 |
| 热点数据 | 用 mock（5 圈子各一条）。真实接入点已封装在 `fetchHotPost()`，替换时必须走服务端，Access Secret 不能进浏览器 |
| 总时长 | 规则写"约 6分50秒"，但逐阶段相加为 450s。以逐阶段为准，可在 `CONFIG.durations` 调 |
| 会话存储 | 未绑定 KV 时用进程内 Map（单实例 Demo 够用，冷启动掉登录态）；绑定 KV 后跨实例安全 |

### 质量验证

18 项规则测试（`node .chk/harness.js`，全部通过）：

- 组队分布 300 次抽样，恒为 3 真人 + 2 AI，且玩家恒为真人
- 黑夜三种判定分支（同选出局 / 分歧平安 / 单 AI 直接生效）
- 胜负三档（2 AI = 伪人完胜 / 1 AI = 平局 / 0 AI = 人类胜）
- 计票最高票出局、NPC 300 次从不投自己
- 套话过滤、昵称前缀剔除、超长截断
- 未配置 API 时的降级路径与日志落库

---

## 四、使用的知乎开放能力

| 能力 | 用法 | 当前状态 |
|---|---|---|
| 知乎 OAuth 登录 | 右上角登录，授权后头像与昵称入场 | 已实现（`/api/oauth/*`，含 state 校验与会话管理） |
| 授权用户基础信息 | `GET openapi.zhihu.com/user`，取 `fullname` / `avatar_path` | 已实现（含 int64 `uid` 无损解析） |
| 知乎热榜 | 按圈子取热点帖作为讨论素材 | **接入点已封装（`fetchHotPost()`），当前为 mock**，替换需走服务端 |

如实说明：热榜目前是 mock 数据，接口调用点已封装好并注明了服务端调用要求。
OAuth 与用户基础信息是真实实现的完整链路。

---

## 五、与知乎社区生态的契合点

**1. 它把知乎最独特的资产变成了玩法。**
知乎的核心不是短视频、不是图片，是**长文本讨论**。这个游戏的全部线索都来自文字表达方式——
除了在知乎，它在哪个平台都不成立。热榜帖、楼中楼、赞同/反对、折叠，都是知乎原生结构，
不是硬贴上来的皮。

**2. "折叠"这个词用对了。**
知乎的折叠机制本身就是社区对低质内容的处理方式。游戏里投票"折叠"伪人，
和社区里折叠灌水回答是同一套语义。玩家在玩的过程中，实际是在练习**社区内容质量判断**。

**3. 它回应了一个社区当下的真实焦虑。**
AI 生成内容涌入是所有内容社区正在面对的问题。这个游戏不做说教，
而是让用户在娱乐中建立对 AI 文本的**具体感知**——知道它哪里像、哪里不像。
比任何"如何识别 AI 文章"的科普都更有效。

**4. 调试台的开放性符合知乎的技术社区气质。**
prompt 可见可改、日志可导出、配置可交换。知乎上会有人写"我把伪人 prompt 调到了 80% 通过率"
这样的分享——这本身就是知乎最典型的内容形态。

---

## 六、对社区用户的实际价值

**对普通用户**：一个 7 分钟的社交推理游戏，玩完会对"AI 写的东西长什么样"有具体认知。
结算页显示你的标记准确率和投票命中率，是可量化的自我验证。

**对创作者**：直观看到 AI 当前能模仿到什么程度，反过来更清楚自己的表达里
哪些部分是不可替代的（个人经历、具体细节、情绪毛刺）。

**对开发者与学生**：一个开箱可用的拟人化 prompt 实验台。
调 prompt、比模型、导出带人工标注的日志——按开源包结构（`config.json` / `prompt.md` /
`adapter.js` / `styles.json` / `logs.json`）可直接抽出来复用为评论区 bot。

**对社区**：每局产生一批"这条 AI 发言像不像人"的人工标注。
积累起来是一份关于中文社区语境下 AI 文本可辨识度的真实数据。

---

## 七、后续规划

| 阶段 | 内容 |
|---|---|
| 近期 | 接真实热榜接口（服务端代理 + 缓存去重）；房间可分享链接 |
| 中期 | WebSocket 多人实时对局，真人混入真人局，5 席可全真人 |
| 中期 | 接入知乎直答，让伪人能引用真实资料，提升伪装难度 |
| 长期 | 沉淀 prompt 与标注数据，开源为独立的评论区 bot 工具包 |
| 长期 | 排行榜：辨认准确率、伪装存活率 |

---

## 附：部署与运行

```bash
npx wrangler login
npx wrangler pages project create zhihu-fog --production-branch main
npx wrangler pages deploy dist
```

接入 OAuth 需额外注入凭证（只能用 secret，不能写进文件）：

```bash
npx wrangler pages secret put ZHIHU_OAUTH_APP_ID       --project-name zhihu-fog
npx wrangler pages secret put ZHIHU_OAUTH_APP_KEY      --project-name zhihu-fog
npx wrangler pages secret put ZHIHU_OAUTH_REDIRECT_URI --project-name zhihu-fog
```

`ZHIHU_OAUTH_REDIRECT_URI` 须与赛事页面登记的回调地址完全一致
（协议、域名、端口、路径、尾部斜杠都算）。

游戏本体 `dist/index.html` 可直接双击离线运行，仅 OAuth 登录按钮需要部署后可用。
